# `expanded_optimizer` — Scenario-Expanded Solver

Alternative formulation: each scenario gets its own set of wind+slack qubits (no
superposition over scenarios). Simpler to understand, but exponentially larger register.

!!! note "Legacy code"
    Written for Qiskit 0.x/1.x. Apply [import fixes](../index.md#installation) before running.

---

## `Optimizer_Expanded`

::: QuantumExpectedValueFunctionProject.expanded_optimizer.Optimizer_Expanded
